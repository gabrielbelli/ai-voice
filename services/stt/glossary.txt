# Two forms:
#
#   heard = intended     a replacement AND a hotword
#   Term                 a hotword only
#
# Use the bare form when the likely mishearing is an ordinary word. "Belli" is
# heard as "belly", but a `belly = Belli` rule would corrupt any sentence that
# genuinely says belly. Biasing the decoder is safe; rewriting is not.
#
# Keep this tight. Every replacement rule is a chance to corrupt a sentence
# where the word was right all along.

# --- replacements: observed mishearings with no innocent reading
cloud code = Claude Code
todd code = Claude Code
entropic = Anthropic
ghost paper = Ghost Pepper
comet = commit
catalaxy = Catallaxy
catalaxi = Catallaxy
theory dashboard = Theoria dashboard
ldr = TLDR
dts = STT
tex to speak = text-to-speech
open sauce = open source
11 labs = ElevenLabs
eleven labs = ElevenLabs

# --- hotwords only: bias the decoder, never rewrite the text
Catallaxy
Theoria
FreeBSD
Belli
Ghostty
Obsidian
Anthropic
Parakeet
