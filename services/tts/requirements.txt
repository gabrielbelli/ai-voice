# CPU only. No torch.
#
# Kokoro is 82M parameters and runs on ONNX Runtime, so the whole image stays
# under half a gigabyte. A torch-based alternative would cost more in wheels
# alone than this ships in total.
kokoro-onnx==0.4.9
onnxruntime==1.24.1
fastapi==0.121.2
uvicorn[standard]==0.38.0
numpy==2.3.4

# soundfile and libsndfile are gone. Every encoder now feeds ffmpeg on a pipe
# or writes the 44 bytes of a RIFF header itself, because a file has to be
# complete before it can be read and that is the whole latency this service was
# removing. Nothing left in the import graph needs it — checked by importing
# the app and encoding all six formats with `soundfile` blocked at the import
# hook — so the library leaves with it.

# The auth middleware, the OpenAI error envelope, the health route and the TLS
# entrypoint, shared with services/stt and services/tts-long instead of
# hand-copied into each. Three copies of app/auth.py drifted into three
# DIFFERENT defects, one per repo; that is what this dependency exists to stop
# happening again.
#
# A PATH into this repository, not a tarball pinned by SHA. The pin was right
# while voice-common was its own repository: a change there reached this
# service only after a push, a tag and a re-pin, and the SHA is what made the
# build reproducible. In one repository the same pin becomes a liability — it
# lets packages/common and the services that embed it sit at different
# revisions inside a single commit, which is the one state a monorepo exists to
# make unrepresentable. The commit is the pin now, and `git log` is the only
# place either version is recorded.
#
# The path is relative to the CURRENT WORKING DIRECTORY, not to this file: pip
# resolves a path requirement against the process's cwd. Checked rather than
# assumed — reading this file from its own directory fails with "Expected
# package name at the start of dependency specifier", which reads like a syntax
# error in the file and is not one. So every install runs from the repository
# root, which is what the Containerfile's WORKDIR and every CI job do.
#
# The [audio] extra is numpy, which is pinned above and already in the image;
# the extra takes no second copy, it only records the dependency honestly.
./packages/common[audio]
