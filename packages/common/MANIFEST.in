# The tests ship in the sdist because voice_common.conformance is meant to be
# run, and a tarball whose own suite cannot run is a tarball nobody checks.
recursive-include tests *.py
include voice-entrypoint.sh
